# CSGO 预测服务配置文件示例
#
# 使用说明:
# 1. 复制本文件为 prediction_service.config
# 2. 根据你的环境修改下面的配置
# 3. 在 prediction_service.py 中读取这个配置

# ============================================================================
# 数据库配置
# ============================================================================

# 数据库服务器地址
DB_HOST=23.254.215.66

# 数据库用户名
DB_USER=root

# 数据库密码
# 注意: 建议使用环境变量或密钥存储，不要直接写在文件里
DB_PASSWORD=Wyj250413.

# 数据库名称
DB_NAME=csgo_trader

# 数据库字符集
DB_CHARSET=utf8mb4

# ============================================================================
# 服务配置
# ============================================================================

# 服务监听地址
# 0.0.0.0 = 监听所有网络接口（允许远程连接）
# 127.0.0.1 = 仅本地连接
SERVICE_HOST=0.0.0.0

# 服务端口
SERVICE_PORT=5001

# Flask 调试模式（开发环境使用，生产环境务必关闭）
FLASK_DEBUG=False

# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================================================
# 缓存配置
# ============================================================================

# 缓存目录路径
CACHE_DIR=/root/csgo_prediction/.cache

# 模型缓存 TTL（小时）
# 超过这个时间的缓存会被清理
CACHE_TTL_HOURS=24

# ============================================================================
# 预测配置
# ============================================================================

# 默认预测天数
DEFAULT_PREDICT_DAYS=7

# 最小历史数据天数
MIN_HISTORY_DAYS=10

# 模型训练数据天数
TRAINING_DATA_DAYS=30

# ============================================================================
# 性能配置
# ============================================================================

# 并发预测的最大批大小（API 限制：1-100）
MAX_BATCH_SIZE=50

# 数据库连接池大小
DB_POOL_SIZE=5

# ============================================================================
# 部署环境
# ============================================================================

# 环境标识: development, production, testing
ENVIRONMENT=production

# ============================================================================
# 外部服务配置
# ============================================================================

# Gunicorn 配置（用于生产部署）
GUNICORN_WORKERS=4
GUNICORN_THREADS=2
GUNICORN_TIMEOUT=120
